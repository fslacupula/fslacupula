# Ejemplo de variables de entorno para producción
# Copia este archivo en Railway → Variables

# Node Environment
NODE_ENV=production

# Server Port (Railway lo asigna automáticamente)
PORT=3001

# JWT Secret (CAMBIAR en producción)
JWT_SECRET=cambiar_esto_por_una_clave_segura_y_larga_aleatoria

# Database (Railway las proporciona automáticamente desde PostgreSQL)
# No necesitas configurarlas manualmente, pero aquí está la referencia:
# DB_HOST=${{Postgres.PGHOST}}
# DB_PORT=${{Postgres.PGPORT}}
# DB_USER=${{Postgres.PGUSER}}
# DB_PASSWORD=${{Postgres.PGPASSWORD}}
# DB_NAME=${{Postgres.PGDATABASE}}
