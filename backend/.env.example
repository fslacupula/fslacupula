PORT=3001

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=tu_password
DB_NAME=futbolclub

# Database Pool Configuration (opcional - con valores por defecto)
DB_POOL_MAX=10              # Máximo conexiones (producción: 20, desarrollo: 10)
DB_POOL_MIN=1               # Mínimo conexiones (producción: 2, desarrollo: 1)
DB_IDLE_TIMEOUT=10000       # Tiempo inactivo en ms (producción: 30000, desarrollo: 10000)
DB_CONNECT_TIMEOUT=5000     # Timeout conexión en ms (producción: 10000, desarrollo: 5000)
DB_MAX_RECONNECT_ATTEMPTS=5 # Intentos de reconexión
DB_RECONNECT_INTERVAL=5000  # Intervalo entre reintentos en ms

# Railway/Production (si usas DATABASE_URL, ignora DB_HOST, DB_PORT, etc.)
# DATABASE_URL=postgresql://user:password@host:port/database

# JWT Configuration
JWT_SECRET=tu_secreto_super_seguro_cambialo

# Application Environment
NODE_ENV=development        # development | production | test
